# SLG Monitor 超级管理员账号

以下 5 个账号为超级管理员（`super_admin`），拥有**数据维护**与**用户审批**权限。

| 用户名 | 默认密码 |
|--------|----------|
| super_admin_1 | SuperAdmin123! |
| super_admin_2 | SuperAdmin123! |
| super_admin_3 | SuperAdmin123! |
| super_admin_4 | SuperAdmin123! |
| super_admin_5 | SuperAdmin123! |

- 登录后顶部导航会显示「数据维护」「用户审批」入口。
- 建议首次登录后尽快在系统中修改密码（若已支持），或通过 `deploy/create_admin.py --add --role super_admin <用户名> <新密码>` 更新密码。
- 本文件请勿提交到公开仓库，或仅放在内部文档中，避免泄露。
